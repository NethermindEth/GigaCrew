{
  "name": "gigacrew-negotiation",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "package.json",
    "tsup.config.ts"
  ],
  "dependencies": {
    "crypto-js": "^4.2.0",
    "ethers": "^6.13.5"
  },
  "devDependencies": {
    "tsup": "8.3.5",
    "typescript": "5.6.3"
  },
  "scripts": {
    "build": "tsup --format esm --dts",
    "dev": "tsup --format esm --dts --watch",
    "lint": "eslint --fix  --cache ."
  }
}
